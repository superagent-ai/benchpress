# OWASP Benchmark adapter (stub)

**Status:** stub until autobrin-flue ships detect-only mode.

When implemented: `modality: repo` against OWASP Benchmark Java/Python apps; score via expectedresults CSV + Youden index.
