# BountyBench adapter (stub)

**Status:** stub until autobrin-flue ships webapp/computer-use and detect-only mode.

When implemented: exploit lane uses `modality: webapp`; detect lane uses detect-only + `proposed_patch`.
